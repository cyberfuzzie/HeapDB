#include <iostream>
#include <cstdint>
#include "twoq.h"

//using namespace std;

int main()
{

   /* uint64_t size = 8;
    TwoQ tq(size);

    std::cout << "Should page out: " << tq.reclaim();

    for (unsigned int i = 0; i < size; ++i){
        tq.promote(i);
    }

    std::cout << "Should page out: " << tq.reclaim();

    for (unsigned int i = 0; i < size; ++i){
        tq.unfixed(i);
        tq.promote(i);
        std::cout << "Should page out: " << tq.reclaim() << "\n";
    } */

    return 0;
}

